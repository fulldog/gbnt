---
description: 改动遇冲突时先恢复本地，拉取最新代码后再重新修改
alwaysApply: true
---

# 冲突时先恢复、拉取、再改

每次改代码前或改动中若出现 **git 冲突**（merge/rebase 冲突、`pull` 被拒、工作区与远端分叉、无法干净合并），**禁止在冲突文件上硬解或继续叠改**。按顺序：

1. **恢复**：把本次未完成/冲突的本地改动还原干净（`git restore` / `git checkout --` 冲突文件；必要时 `git merge --abort` / `git rebase --abort`）。不要用 `reset --hard`、不要 `push --force`，除非用户明确要求。
2. **拉取最新**：`git fetch` 后对当前跟踪分支 `git pull`（或用户指定的远端分支），确保工作区基于最新提交。
3. **重新修改**：对照用户需求，在最新代码上重做改动；不要把旧冲突块原样贴回去。

无冲突时正常改即可，不必无故 restore/pull。
