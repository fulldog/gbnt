# 小程序无法使用 Vite 开发代理，必须配置完整的后端 Origin（不要包含结尾斜杠）。
VITE_API_BASE_URL=https://nt.kfqzhsq.cn:8443

# 复制到未提交的 .env.local；发布到其他环境时替换为对应的 HTTPS Origin。
# 如存在 .env.development.local，也需同步更新其中的地址并保留 8443 端口。
# 微信公众平台需配置 request、uploadFile、downloadFile 合法域名，并保持域名校验开启。
