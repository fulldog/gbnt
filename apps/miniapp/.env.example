# 小程序无法使用 Vite 开发代理，必须配置完整的后端 Origin（不要包含结尾斜杠）。
VITE_API_BASE_URL=https://api.example.com

# 本地联调测试服务时，在未提交的 .env.local 中显式设置：
# VITE_API_BASE_URL=http://www.weilone.com
# 同时仅在微信开发者工具本地调试时临时关闭合法域名校验。
